a, th
	cursor pointer

header
	label:first-of-type
		color #666
		font-weight normal
		diplay inline-block
		width 200px
	input[type=radio] 
		& + label
			font-weight normal
			color #666
		&:checked + label
			color #000
